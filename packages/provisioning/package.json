{
  "name": "@openbika/provisioning",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@openbika/contracts": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "vitest": "^4.0.15"
  }
}
